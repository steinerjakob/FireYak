.DS_Store
node_modules
/dist
authkey.json
# local env files
.env.local
.env.*.local

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
/play-credentials.json

# Local SSH keys (e.g. GitHub Actions deploy keys) — never commit
id_rsa*

# TypeScript build cache
tsconfig.tsbuildinfo

# Claude Code local state (project skills stay shared)
.claude/*
!.claude/skills/
